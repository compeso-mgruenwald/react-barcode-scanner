# Example

Vite+ app for trying `@thewirv/react-barcode-scanner` in a browser.

```bash
bun install
bun run dev
```

`bun run check` runs Vite+ check (fmt + lint + types), then `vp build`. Settings live in `vite.config.ts`.
